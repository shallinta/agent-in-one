{
  "name": "@pair-agent/source-lock",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=22.19.0",
    "pnpm": "11.7.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "verify": "node scripts/verify.mjs",
    "test": "pnpm run test:source && pnpm -r --if-present test",
    "test:source": "vitest run",
    "typecheck": "pnpm -r --if-present typecheck && pnpm run typecheck:root",
    "typecheck:root": "tsc --noEmit -p tsconfig.base.json",
    "build": "pnpm -r --if-present --filter '!@pair-agent/pair-web' build && pnpm --filter @pair-agent/pair-web build:check && pnpm run build:root",
    "build:root": "node --check scripts/prepare-dsh.mjs && node --check scripts/verify-dsh.mjs && node --check scripts/refresh-dsh-runtime-artifacts.mjs && node --check scripts/dev.mjs && node --check scripts/verify.mjs",
    "prepare:source": "node scripts/prepare-dsh.mjs",
    "verify:source": "node scripts/verify-dsh.mjs",
    "refresh:runtime-artifacts": "node scripts/refresh-dsh-runtime-artifacts.mjs --write-lock"
  },
  "devDependencies": {
    "@types/node": "22.18.3",
    "typescript": "5.9.2",
    "vitest": "2.1.9"
  }
}
