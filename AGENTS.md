# 仓库说明

## 仓库用途

这是一个个人 AI 学习仓库，用于持续学习、探索和记录 AI 相关的概念、工具、工程实践与代码实验。

仓库内容以学习过程和探索结果的存档为主，不要求形成统一的应用、工程结构或运行方式。

## 目录说明

- `posts/`：存放学习文章和整理后的知识记录。
- 其他根目录子目录：存放独立的专题探索和代码尝试。这些目录可能包含代码，也可能只包含文档、素材或其他研究产物。

## 更新规则

### 增加学习文章

1. 新文章应放在 `posts/` 目录下。
2. 如果一篇文章包含图片、附件或其他多个文件，应在 `posts/` 下创建独立文件夹统一存放。
3. 增加文章后，应同步更新根目录 `README.md` 中的文章索引。

### 增加其他学习探索

1. 文章以外的学习探索，应在仓库根目录创建独立目录存档。
2. 探索目录可以包含代码，也可以不包含代码。
3. 不需要在仓库根目录配置统一的安装、启动或构建命令；如有运行说明，应由对应探索目录自行记录。
4. 增加探索目录后，应同步更新根目录 `README.md` 的“探索目录”章节，补充探索名称和简短介绍；如果该章节尚不存在，应先创建。
